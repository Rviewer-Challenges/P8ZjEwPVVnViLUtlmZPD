import 'package:flutter/cupertino.dart';

class DetailItemBody extends StatelessWidget {
  const DetailItemBody({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
